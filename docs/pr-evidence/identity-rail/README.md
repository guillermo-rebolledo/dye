# Identity rail

Screenshots for the identity-rail change.

**The captures were removed.** All three showed Apple's iOS simulator sample
photograph, republished at full resolution with no attribution, and all three were
stale against `HEAD`: they showed the pre-`cde0c7f` `"Stock"` headline and an
`"Identity"` cell, neither of which the app has said for some time. See
[`../README.md`](../README.md) for what a replacement must be captured against.
